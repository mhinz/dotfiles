#!/bin/sh

cd /data/repo/git
make distclean 1>/dev/null
make configure 1>/dev/null

if git pull | grep 'Already up-to-date.'; then
    [[ $1 == '-f' ]] && ./configure --prefix=$HOME/local/git && make install install-doc
else
    ./configure --prefix=$HOME/local/git && make install install-doc
fi
